Q1. 부서(dept)테이블과 사원(emp)테이블이 있을때 부서테이블에서 사용할 수 있는 연관관계는?

1. @ManyToOne @JoinColumn(name = "emp_no")
2. @OneToMany(mappedby = "emp")
3. @OneToOne
4. @ManyToMany


Q2. JPA에서 데이터베이스에 작업을 지시하는 주체는?

1. 엔티티

2. 엔티티 매니저

3. 엔티티 매니저 팩토리

4. 엔티티 컨텍스트


Q3. 영속성 컨텍스트는 (      )을(를) 영구 저장하는 환경이다.
(     )를 통해서 영속성 컨텍스트에 접근할 수 있고, 영속성 컨텍스트를 관리 할 수 있다.


Q4. 특정 엔티티를 조회할 때 연관된 모든 엔티티를 같이 로딩하는 방식을 일컫는 말은?


Q5. 불필요한 조인까지 포함해 처리하는 문제를 해결하기 위해 ( )를 사용한다
