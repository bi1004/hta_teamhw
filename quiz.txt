Q1. 부서(dept)테이블과 사원(emp)테이블이 있을때 부서테이블에서 사용할 수 있는 연관관계는?

1. @ManyToOne @JoinColumn(name = "emp_no")
2. @OneToMany(mappedby = "emp")
3. @OneToOne
4. @ManyToMany

정답 : 2번

[해설] 
부모테이블(pk)에서 자식테이블(fk)을 매핑할땐 1:다 관계를 사용한다.

1. 다:1

2. 1:다

3. 1:1

4. 다:다


Q2. JPA에서 데이터베이스에 작업을 지시하는 주체는?

1. 엔티티

2. 엔티티 매니저

3. 엔티티 매니저 팩토리

4. 엔티티 컨텍스트


정답 : 2번

1. 엔티티는 테이블에 대응하는 하나의 클래스와 같은 개념

2. 엔티티를 저장, 수정, 삭제, 조회등 엔티티와 관련된 모든 일을 처리

3. 말그대로 엔티티 매니저를 만드는 공장

4. 애플리케이션과 데이터베이스 사이에서 객체를 
보관하는 가상의 데이터베이스 같은 역할



Q3. 영속성 컨텍스트는 (      )을(를) 영구 저장하는 환경이다.
(     )를 통해서 영속성 컨텍스트에 접근할 수 있고, 영속성 컨텍스트를 관리 할 수 있다.

정답 : 엔티티(Entity), 엔티티 매니저(EntityManager)



Q4. 특정 엔티티를 조회할 때 연관된 모든 엔티티를 같이 로딩하는 방식을 일컫는 말은?
정답: 즉시로딩

Q5. 불필요한 조인까지 포함해 처리하는 문제를 해결하기 위해 ( )를 사용한다
정답: 지연로딩